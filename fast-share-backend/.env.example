# FastShare Node.js backend - copy to .env and adjust

# Server port
PORT=8000

# SQLite database path (relative to project root or absolute)
DB_PATH=app.db

# CORS: comma-separated list of allowed frontend origins (no spaces after commas)
# Include the exact URL your frontend runs on (with port)
CORS_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:5173,https://your-app.example.com
